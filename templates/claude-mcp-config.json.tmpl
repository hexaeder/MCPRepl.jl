{
  "mcpServers": {
    "julia-repl": {
      "type": "http",
      "url": "http://localhost:\${JULIA_MCP_PORT}",
      "headers": {{% if has_api_key %}
        "Authorization": "Bearer \${JULIA_MCP_API_KEY}",{% end %}
        "X-MCPRepl-Session": "\${JULIA_MCP_SESSION}"
      }
    }
  }
}
