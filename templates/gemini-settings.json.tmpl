{
  "mcpServers": {
    "julia-repl": {
      "httpUrl": "http://localhost:${JULIA_MCP_PORT}",
      "headers": {
        "X-MCPRepl-Session": "${JULIA_MCP_SESSION}"{{#if has_api_key}},
        "Authorization": "Bearer ${JULIA_MCP_API_KEY}"{{/if}}
      }
    }
  }
}
