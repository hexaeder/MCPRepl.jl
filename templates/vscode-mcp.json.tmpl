{
  "servers": {
    "julia-repl": {
      "type": "http",
      "url": "http://localhost:{{port}}"{% if has_api_key %},
      "headers": {
        "Authorization": "Bearer {{api_key}}",
        "X-MCPRepl-Target": "{{repl_id}}"
      }{% else %},
      "headers": {
        "X-MCPRepl-Target": "{{repl_id}}"
      }{% end %}
    }
  },
  "inputs": []
}
