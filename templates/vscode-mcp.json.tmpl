{
  "servers": {
    "julia-repl": {
      "type": "http",
      "url": "http://localhost:{{port}}",
      "headers": {{% if has_api_key %}
        "Authorization": "Bearer {{api_key}}",{% end %}
        "X-MCPRepl-Session": "{{session_name}}"
      }
    }
  },
  "inputs": []
}
